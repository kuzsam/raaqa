# RAAQA — Read Alignment-based Assembly Quality Assessment

name: raaqa
channels:
  - conda-forge
  - bioconda
  - nodefaults
dependencies:
  - python>=3.10

  # --- Mapq_softclip module dependency ---
  - pysam>=0.22.0

  # --- Visualise module dependencies ---
  - matplotlib-base>=3.8
  - pandas>=2.0
  - numpy>=1.26

  # --- Testing ---
  - pytest>=7.0

  # --- Preprocessing tools ---
  # Required to generate inputs for mapq_softclip module:
  - samtools>=1.17
  # Required to generate inputs for hese module:
  #